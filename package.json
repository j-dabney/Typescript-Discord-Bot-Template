{
  "name": "bottemplate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -r -f build",
    "build": "tsc --build",
    "start": "npm run clean && npm run build && node build/index.js",
    "startNoClean": "npm run build && node builds/index.js",
    "devStart": "npm run clean && npm run build && node build/index.js --development",
    "devStartNoClean": "npm run build && node builds/index.js --development"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^14.14.1",
    "dotenv": "^16.4.5",
    "glob": "^10.3.10",
    "mongoose": "^8.2.1"
  },
  "devDependencies": {
    "typescript": "^5.4.2"
  }
}
